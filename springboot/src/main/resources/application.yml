spring:
  application:
    name: 536
  activemq:
    # activemq url
    broker-url: tcp://localhost:61616
    in-memory: true
    pool:
      # 如果此处设置为true，需要添加activemq-pool的依赖包，否则会自动配置
      enabled: false

  jms:
    pub-sub-domain: true


  redis:
    host: 120.78.93.113
    port: 6365
    password: CCCgiggle123...